{% extends 'base.html.twig' %}

{% block title %}VOIR LES CLES CRYPTES{% endblock %}

{% block body %}

    <div class="container w-50 mx-auto my-5">
        <h1 class="bg-primary mt-5 mb-0 py-2 text-center">VOIR LES CLES CRYPTES ACTUELLES :</h1>
        <a href="{{ path('app_dataHome')}}" class="btn btn-primary mt-3">Retour</a>
        <table class="table table-striped table-hover text-left my-5">
            <thead>
                <tr class="bg-secondary">
                    <th scope="col" class="text-center">#</th>
                    <th scope="col" class="text-center">Nom</th>
                    <th scope="col" class="text-center">Clé actuelle</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th scope="row" class="text-center">#</th>
                    <td class="text-center"><strong>Revendeur</strong></td>
                    <td class="text-center"><strong>{% if cryptedKeyR is empty %}{% else %}{{ cryptedKeyR.cle }}{% endif %}</strong></td>
                </tr>
                <tr>
                    <th scope="row" class="text-center">#</th>
                    <td class="text-center"><strong>Webshop</strong></td>
                    <td class="text-center"><strong>{% if cryptedKeyW is empty %}{% else %}{{ cryptedKeyW.cle }}{% endif %}</strong></td>
                </tr>
            </tbody>            
        </table>       
    </div>

{% endblock %}